fastapi>=0.115
uvicorn>=0.30
sqlalchemy>=2.0
pyyaml>=6.0
sqlglot>=25.0
openpyxl>=3.1
python-multipart>=0.0.9
anthropic>=0.40
pytest>=8.0
httpx>=0.27
# Drivers según el ERP del cliente (descomentar el que corresponda):
# pyodbc          # SQL Server (Tango, Bejerman, SAP B1 sobre SQL Server)
# psycopg[binary] # PostgreSQL (Odoo)
# pymysql         # MySQL / MariaDB
# oracledb        # Oracle
